# 프로젝트 개요

[프로젝트 설명을 여기에 작성하세요]

기술 스택:
- [프레임워크]
- [언어]
- [데이터베이스]

## 구현 규칙

- /plan 단계에서 테스트 코드 포함 여부를 먼저 결정한다
- 포함하기로 했다면, 구현 전에 테스트를 먼저 작성하고 구현 후 통과 여부를 확인한다
- 테스트가 실패하면 통과할 때까지 코드를 수정한다
- 포함하기로 한 테스트를 삭제하거나 skip 처리해서는 안 된다

코드 수정 후 반드시 실행:

```bash
# 프로젝트에 맞게 수정하세요
npm run lint
npx tsc --noEmit
npm test
```

## 세션 시작 시 필수 확인

새 세션이 시작되면 코드에 손대기 전에 반드시 아래 파일들을 읽는다:

1. `.claude/commands/` 디렉토리의 모든 `.md` 파일
2. `.claude/plans/` 디렉토리 — 활성 계획 파일 확인
