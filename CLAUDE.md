# 프로젝트 개요

[프로젝트 설명을 여기에 작성하세요]

기술 스택:
- [프레임워크]
- [언어]
- [데이터베이스]

## 구현 규칙

- 새 기능을 구현하기 전에 반드시 테스트를 먼저 작성한다
- 구현 후 테스트가 통과하는지 확인한다
- 테스트가 실패하면 통과할 때까지 코드를 수정한다
- 테스트를 삭제하거나 skip 처리해서는 안 된다

코드 수정 후 반드시 실행:

```bash
# 프로젝트에 맞게 수정하세요
npm run lint
npx tsc --noEmit
npm test
```

## Stop Hook 피드백 처리 규칙

Stop hook 피드백 메시지가 오면 반드시 아래 순서로만 처리한다:

1. `bash .claude/hooks/policy-check.sh` 즉시 실행
2. exit 0 → 아무 텍스트도 출력하지 않는다 (침묵)
3. exit 2 → 테스트 없는 파일에 테스트 작성 후 재실행

텍스트로 응답하는 것은 Stop 이벤트를 재트리거하므로 금지한다.

## 세션 시작 시 필수 확인

새 세션이 시작되면 코드에 손대기 전에 반드시 아래 파일들을 읽는다:

1. `.claude/commands/` 디렉토리의 모든 `.md` 파일
2. `.claude/plans/` 디렉토리 — 활성 계획 파일 확인
